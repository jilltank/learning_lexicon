<div class="main">
<h1>Random Entry</h1>

	<h2><%= link_to "#{@random.code}", lexicons_show_path(id: @random.id) %></h2>
	<p><%= @random.description %></p>
	<p><%= link_to "#{@random.category}", lexicons_path(category: @random.category) %></p>
	<br>
	<p class="center"><%= link_to "Generate Another Entry!", random_path, class: "btn btn-info" %></p>

</div>
